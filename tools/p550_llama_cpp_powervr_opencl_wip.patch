diff --git a/ggml/src/ggml-opencl/ggml-opencl.cpp b/ggml/src/ggml-opencl/ggml-opencl.cpp
index 000000000..000000000 100644
--- a/ggml/src/ggml-opencl/ggml-opencl.cpp
+++ b/ggml/src/ggml-opencl/ggml-opencl.cpp
@@ -88,6 +88,7 @@ enum GPU_FAMILY {
 enum GPU_FAMILY {
     ADRENO,
     INTEL,
+    POWERVR,
     UNKNOWN,
 };
@@ -848,6 +849,12 @@ struct ggml_backend_opencl_context {
     ...
 };

+static inline bool ggml_opencl_is_powervr(const ggml_backend_opencl_context *backend_ctx) {
+    return backend_ctx->device_name.find("PowerVR") != std::string::npos ||
+           backend_ctx->device_name.find("AXM") != std::string::npos;
+}
+
 static std::vector<ggml_backend_device> g_ggml_backend_opencl_devices;
@@ -1009,6 +1016,13 @@ static void load_cl_kernels(ggml_backend_opencl_context *backend_ctx) {
     std::string compile_opts = std::string("-cl-std=") + opencl_c_std +
                                " -cl-mad-enable -cl-unsafe-math-optimizations"
                                " -cl-finite-math-only -cl-fast-relaxed-math";
+
+    if (ggml_opencl_is_powervr(backend_ctx)) {
+        compile_opts += " -DINTEL_GPU=1"
+                        " -DREQD_SUBGROUP_SIZE_16="
+                        " -DREQD_SUBGROUP_SIZE_32=";
+    }
+
     if (backend_ctx->adreno_use_large_buffer) {
         compile_opts += " -qcom-enable-large-buffer ";
     }
@@ -1016,6 +1030,25 @@ static void load_cl_kernels(ggml_backend_opencl_context *backend_ctx) {
+    if (ggml_opencl_is_powervr(backend_ctx)) {
+        GGML_LOG_INFO("ggml_opencl: loading minimal PowerVR OpenCL kernels");
+#ifdef GGML_OPENCL_EMBED_KERNELS
+        const std::string kernel_src {
+            #include "mul_mv_f16_f32.cl.h"
+        };
+#else
+        const std::string kernel_src = read_file("mul_mv_f16_f32.cl");
+#endif
+        std::string f16_f32_compile_opts = compile_opts + " -DPOWERVR_GPU=1";
+        backend_ctx->program_mul_mv_f16_f32 =
+            build_program_from_source(backend_ctx->context, backend_ctx->device,
+                                      kernel_src.c_str(), f16_f32_compile_opts);
+        CL_CHECK((backend_ctx->kernel_mul_mat_f16_f32 =
+                      clCreateKernel(backend_ctx->program_mul_mv_f16_f32,
+                                     "kernel_mul_mat_f16_f32", &err), err));
+        backend_ctx->kernels_loaded = true;
+        GGML_LOG_CONT(".\n");
+        return;
+    }
+
     GGML_LOG_INFO("ggml_opencl: loading OpenCL kernels");
@@ -3922,6 +3950,13 @@ static bool ggml_opencl_is_device_supported(ggml_backend_dev_t dev) {
     } else if (strstr(dev_ctx->device_name.c_str(), "Intel")) {
         dev_ctx->gpu_family = GPU_FAMILY::INTEL;
+    } else if (strstr(dev_ctx->device_name.c_str(), "PowerVR") ||
+               strstr(dev_ctx->device_name.c_str(), "AXM") ||
+               strstr(dev_ctx->platform_name.c_str(), "PowerVR") ||
+               strstr(dev_ctx->platform_name.c_str(), "Imagination")) {
+        dev_ctx->gpu_family = GPU_FAMILY::INTEL;
     } else {
         GGML_LOG_WARN("ggml_opencl: unsupported GPU '%s'.\n", dev_ctx->device_name.c_str());
         dev_ctx->gpu_family = GPU_FAMILY::UNKNOWN;
@@ -4988,6 +5023,22 @@ static bool ggml_opencl_supports_op(ggml_backend_dev_t dev, const struct ggml_tensor * op) {
         return false;
     }

+    if (ggml_opencl_is_powervr(backend_ctx)) {
+        switch (op->op) {
+            case GGML_OP_NONE:
+            case GGML_OP_RESHAPE:
+            case GGML_OP_VIEW:
+            case GGML_OP_PERMUTE:
+            case GGML_OP_TRANSPOSE:
+                return true;
+            case GGML_OP_MUL_MAT:
+                return op->src[0]->type == GGML_TYPE_F16 &&
+                       op->src[1]->type == GGML_TYPE_F32;
+            default:
+                return false;
+        }
+    }
+
     switch (op->op) {
@@ -13928,10 +13976,15 @@ static void ggml_cl_mul_mat(ggml_backend_t backend, const ggml_tensor * src0,
         case GGML_TYPE_F16:
             //GGML_ASSERT(ne02 == ne12);
-            if (backend_ctx->gpu_family == INTEL) {
+            if (ggml_opencl_is_powervr(backend_ctx)) {
+                nth0 = 1;
+                nth1 = 1;
+            } else if (backend_ctx->gpu_family == INTEL) {
                 nth0 = 32;
                 nth1 = 1;
             } else if (backend_ctx->gpu_family == ADRENO) {
@@ -13940,7 +13993,10 @@ static void ggml_cl_mul_mat(ggml_backend_t backend, const ggml_tensor * src0,
             }

             if (src1t == GGML_TYPE_F32) {
-                if (ne11 * ne12 < 4) {
+                if (ggml_opencl_is_powervr(backend_ctx)) {
+                    kernel = backend_ctx->kernel_mul_mat_f16_f32;
+                    nrows = 4;
+                } else if (ne11 * ne12 < 4) {
                     kernel = backend_ctx->kernel_mul_mat_f16_f32_1row;
                 } else if (ne00 >= 128 && ne01 >= 8 && ne00%4 == 0) {
                     kernel = backend_ctx->kernel_mul_mat_f16_f32_l4;
diff --git a/ggml/src/ggml-opencl/kernels/mul_mv_f16_f32.cl b/ggml/src/ggml-opencl/kernels/mul_mv_f16_f32.cl
index 000000000..000000000 100644
--- a/ggml/src/ggml-opencl/kernels/mul_mv_f16_f32.cl
+++ b/ggml/src/ggml-opencl/kernels/mul_mv_f16_f32.cl
@@ -65,6 +65,28 @@ kernel void kernel_mul_mat_f16_f32(

     global half * x = (global half *) (src0 + offset_src0);

+#ifdef POWERVR_GPU
+    for (int row = 0; row < N_F16_F32; ++row) {
+        int r1 = rb + row;
+        if (r1 >= ne11) {
+            break;
+        }
+
+        ulong offset_src1 = r1*nb11 + (i12   )*nb12 + (i13   )*nb13;
+        global float * y = (global float *) (src1 + offset_src1);
+
+        float sumf = 0.0f;
+        for (int i = 0; i < ne00; ++i) {
+            sumf += convert_float(x[i]) * y[i];
+        }
+        dst[im*ne1*ne0 + r1*ne0 + r0] = sumf;
+    }
+    return;
+#endif
+
     if (ne00 < 128) {
diff --git a/ggml/src/ggml-opencl/kernels/mul_mv_q4_k_f32.cl b/ggml/src/ggml-opencl/kernels/mul_mv_q4_k_f32.cl
index 000000000..000000000 100644
--- a/ggml/src/ggml-opencl/kernels/mul_mv_q4_k_f32.cl
+++ b/ggml/src/ggml-opencl/kernels/mul_mv_q4_k_f32.cl
@@ -1,3 +1,4 @@
+#pragma OPENCL EXTENSION cl_khr_fp16 : enable
 #ifdef cl_intel_required_subgroup_size
 #pragma OPENCL EXTENSION cl_intel_required_subgroup_size : enable
diff --git a/ggml/src/ggml-opencl/kernels/gated_delta_net.cl b/ggml/src/ggml-opencl/kernels/gated_delta_net.cl
index 000000000..000000000 100644
--- a/ggml/src/ggml-opencl/kernels/gated_delta_net.cl
+++ b/ggml/src/ggml-opencl/kernels/gated_delta_net.cl
@@ -1,3 +1,4 @@
+#pragma OPENCL EXTENSION cl_khr_fp16 : enable
diff --git a/ggml/src/ggml-opencl/kernels/l2_norm.cl b/ggml/src/ggml-opencl/kernels/l2_norm.cl
index 000000000..000000000 100644
--- a/ggml/src/ggml-opencl/kernels/l2_norm.cl
+++ b/ggml/src/ggml-opencl/kernels/l2_norm.cl
@@ -1,3 +1,4 @@
+#pragma OPENCL EXTENSION cl_khr_fp16 : enable
diff --git a/ggml/src/ggml-opencl/kernels/tsembd.cl b/ggml/src/ggml-opencl/kernels/tsembd.cl
index 000000000..000000000 100644
--- a/ggml/src/ggml-opencl/kernels/tsembd.cl
+++ b/ggml/src/ggml-opencl/kernels/tsembd.cl
@@ -1,3 +1,4 @@
+#pragma OPENCL EXTENSION cl_khr_fp16 : enable
